# Setup for winrm

open up powershell as admin and type:
winrm quickconfig

if it says make these chnages press y then hit enter then after that type:
Enable-PSRemoting -Force
it takes some time but then close powershell and open up the exe and it should run
